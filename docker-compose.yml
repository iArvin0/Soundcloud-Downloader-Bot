services:
  bot:
    build: .
    container_name: soundcloud-downloader-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./downloads:/app/downloads
